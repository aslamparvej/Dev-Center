<%- include('../includes/head', {webTitle: 'Where Code Meets Creativity' }) %>
    <%- include('../includes/header') %>

        <!-- Home Container :: Begins -->
        <main>
            <section class="hero" id="home">
                <div class="container">
                    <div class="hero-content">
                        <h1>AI-Powered Software Development Blog</h1>
                        <p>Discover cutting-edge insights, tutorials, and AI-generated content that will elevate your
                            coding journey. From beginner tips to advanced architectures.</p>
                        <a href="#blog" class="cta-button">
                            Explore Articles
                            <span>→</span>
                        </a>
                    </div>
                </div>
            </section>

            <section class="features">
                <div class="container">
                    <h2>What Makes Us Different</h2>
                    <div class="features-grid">
                        <div class="feature-card">
                            <span class="feature-icon">🤖</span>
                            <h3>AI-Enhanced Content</h3>
                            <p>Our articles are crafted with the help of advanced AI, ensuring comprehensive coverage of
                                complex topics with clarity and precision.</p>
                        </div>
                        <div class="feature-card">
                            <span class="feature-icon">💡</span>
                            <h3>Practical Insights</h3>
                            <p>Every post includes real-world examples, code snippets, and actionable advice that you
                                can immediately apply to your projects.</p>
                        </div>
                        <div class="feature-card">
                            <span class="feature-icon">🚀</span>
                            <h3>Latest Technologies</h3>
                            <p>Stay ahead of the curve with coverage of emerging frameworks, languages, and development
                                methodologies as they evolve.</p>
                        </div>
                    </div>
                </div>
            </section>

            <section class="blog-preview" id="blog">
                <div class="container">
                    <h2>Latest Articles</h2>
                    <div class="blog-grid">
                        <% blogs.forEach(function(blog) { %>
                            <article class="article-card" data-category="node" data-date="2024-03-15"
                                data-popularity="850" data-readtime="8"
                                onclick="location.href='/blog/<%= blog.slug %>'">
                                <div class="article-image">
                                    <img src="<%= blog.featuredImagePath %>" alt="<%= blog.title %>" />
                                    <div class="ai-badge">AI Generated</div>
                                </div>
                                <div class="article-content">
                                    <div class="article-meta">
                                        <div class="article-tags">
                                            <span class="article-tag">Programming & Development</span>
                                        </div>
                                        <div class="article-date">📅 <%= moment(blog.createdAt).format(' DD MMM YYYY')
                                                %>
                                        </div>
                                    </div>
                                    <h3 class="article-title">
                                        <%= blog.title %>
                                    </h3>
                                    <p class="article-excerpt"><%= blog.excerpt %></p>
                                    <div class="article-footer">
                                        <div class="read-time">
                                            <%= blog.readingMinutes %> min read
                                        </div>
                                        <p class="read-more">Read More →</a>
                                    </div>
                                </div>
                            </article>

                            <% }); %>
                    </div>
                </div>
            </section>

            <section class="newsletter">
                <div class="container">
                    <h2>Stay Updated</h2>
                    <p>Get the latest articles and AI-powered insights delivered directly to your inbox. Join our
                        community of developers.</p>
                    <form class="newsletter-form" onsubmit="handleSubmit(event)">
                        <input type="email" placeholder="Enter your email" required>
                        <button type="submit">Subscribe</button>
                    </form>
                </div>
            </section>
        </main>
        <!-- Home Container :: Begins -->

        <script>
            // Smooth scrolling for navigation links
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', function (e) {
                    e.preventDefault();
                    const target = document.querySelector(this.getAttribute('href'));
                    if (target) {
                        target.scrollIntoView({
                            behavior: 'smooth',
                            block: 'start'
                        });
                    }
                });
            });

            // Header background on scroll
            window.addEventListener('scroll', () => {
                const header = document.querySelector('header');
                if (window.scrollY > 100) {
                    header.style.background = 'rgba(15, 23, 42, 0.98)';
                } else {
                    header.style.background = 'rgba(15, 23, 42, 0.95)';
                }
            });

            // Newsletter form submission
            function handleSubmit(event) {
                event.preventDefault();
                const email = event.target.querySelector('input[type="email"]').value;
                const button = event.target.querySelector('button');

                button.textContent = 'Subscribed!';
                button.style.background = '#10b981';

                setTimeout(() => {
                    button.textContent = 'Subscribe';
                    button.style.background = '#3b82f6';
                    event.target.reset();
                }, 2000);
            }

            // Add intersection observer for animations
            const observerOptions = {
                threshold: 0.1,
                rootMargin: '0px 0px -50px 0px'
            };

            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.style.opacity = '1';
                        entry.target.style.transform = 'translateY(0)';
                    }
                });
            }, observerOptions);

            document.querySelectorAll('.feature-card, .blog-card').forEach(el => {
                observer.observe(el);
            });
        </script>

        <%- include('../includes/footer') %>